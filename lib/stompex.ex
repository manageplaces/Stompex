defmodule Stompex do
  @doc """
  """

  @doc """
  """
  def connect(host, login, passcode) do
  end

  def connect(host, port, login, passcode) do
  end

  def connect(host, port, login, passcode, headers) do
  end

end
